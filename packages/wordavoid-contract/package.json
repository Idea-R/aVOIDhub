{
  "name": "@avoid/wordavoid-contract",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "generate:dictionary": "node tools/generate-dictionary.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest --run",
    "verify": "npm run generate:dictionary -- --check && npm run typecheck && npm run test"
  },
  "devDependencies": {
    "typescript": "~5.8.3",
    "vitest": "^4.1.10"
  }
}
